🚀 VM SETUP - COPY-PASTE INSTRUCTIONS
Access the VM: Open http://34.171.231.16:8006 in your browser

Once you see the Windows desktop, open PowerShell as Admin (Right-click Start → Windows Terminal (Admin)) and paste this single command:


Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://chocolatey.org/install.ps1')); choco install python311 git vscode -y; $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine"); cd C:\; git clone https://github.com/amuzetnoM/cthulu.git; cd cthulu; pip install -r requirements.txt; if (!(Test-Path "config.json")) { Copy-Item "config.example.json" "config.json" }; Write-Host "DONE! Install MT5 manually, then run: python __main__.py --wizard" -ForegroundColor Green
Then manually:

Download MT5 from https://www.metatrader5.com/en/download
Install & login to your broker
Enable Algo Trading: Tools → Options → Expert Advisors → ✓ Allow Algo Trading
Edit C:\cthulu\config.json with your settings
Run: python __main__.py --wizard
Would you like me to:

Connect to the VM via gcloud SSH to check its status?
Create a scheduled task script for auto-starting Cthulu on VM boot?
Set up VS Code Server for remote development?